# Netlify discovers this repository-level file when the site is connected to GitHub.
# The site-specific build, functions, headers, and redirects live with the frontend.
[build]
  base = "icarecenter-frontend"
